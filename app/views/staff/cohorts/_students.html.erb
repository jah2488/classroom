<section>
  <header>
    <h3><%= t('.students') %></h3>
  </header>
  <ul class='list'>
    <% students.each do |student| %>
      <li><%= link_to student, student %></li>
    <% end.empty? and begin %>
      <p> No Students </p>
    <% end %>
  </ul>
</section>
