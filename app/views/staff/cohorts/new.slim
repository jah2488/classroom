.col-lg-4.col-lg-offset-4
  h1 Create Cohort
  = simple_form_for [:staff, cohort] do |f|
    .form-inputs
      = f.input :name
      = f.association :campus
      = f.input :instructor_id, collection: Instructor.all, selected: current_user.instructor
      = f.input :start_date, as: :date
    .form-actions
      = f.button :submit
